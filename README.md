# Pulse360Website

Pulse 360 marketing intelligence website built with React and Vite.

## Run locally

```bash
npm install
npm start
```

Then open the local URL shown in the terminal.

For development with Vite's default command:

```bash
npm run dev
```

## Build

```bash
npm run build
```
